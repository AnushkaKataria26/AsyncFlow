[pytest]
markers =
    integration: requires all infrastructure running
    crash_simulation: kills processes, may affect system state, run in isolation
    flaky: timing-sensitive, occasional false failures expected
